# Example domains
# Used by middleware only to avoid loops if you deploy the same app on the mobile host too
MOBILE_HOST=m.example.com

# Client-side redirect target (can be a totally different domain)
NEXT_PUBLIC_MOBILE_URL=https://m.example.com
